/**
 * 公式函数注册表
 *
 * - **函数来源**：[@formulajs/formulajs](https://formulajs.info/functions/)（Excel 兼容，非平台自定义）
 * - **分类来源**：`formulajs-catalog.json`（由 formulajs.info 导出，可用 scripts 重新生成）
 * - **UI 文案**：`meta.ts` 按需覆盖 label / hint / featured / implemented
 * - **求值引擎**：仅 `implemented: true` 的函数接入 `evaluate.ts`
 *
 * 后续对照语雀完善：在 `meta.ts` 增加条目即可，不必改引擎或菜单结构。
 */
