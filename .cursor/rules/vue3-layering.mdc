---
description: speed-sheet Vue 分层、headless 边界、vue3-antd 开发约定（改 vue3 / antd / SpeedSheet 时必读）
alwaysApply: true
globs:
  - speed-sheet/packages/**/*
  - speed-sheet/demos/**/*
---

# speed-sheet Vue 架构约定

## 包依赖（禁止反向依赖）

```
@speed-sheet/core          ← Sheet、chain、renderSheet、interaction Session
        ↓
@speed-sheet/view          ← SheetViewport：滚动、绘制调度、指针/键盘/拖拽（无框架）
        ↓
@speed-sheet/vue3          ← SheetCanvas + useSheetCanvasView + useSheetInlineEdit
        ↓
@speed-sheet/vue3-antd     ← 默认产品皮：公式栏 / 工具栏 / 页签 / antd 菜单
```

- **禁止**在 vue3 恢复 `useSheetCanvasDraw`、`useSheetLayout` 等已删除的薄 composable；视口逻辑加在 **`@speed-sheet/view`**，Vue 只改 **`useSheetCanvasView`**。
- **React** 应直接依赖 `view`，不要抄 vue3 hook。

- **vue3-element**（规划）只依赖 `vue3`，与 `vue3-antd` **平行**，互不依赖。
- **业务 API 唯一句柄**：`sheet` 实例（对齐 Tiptap `editor`），不要在外壳层再包一层 `getRange` 等到 `defineExpose`。

## @speed-sheet/vue3（headless）

| 保留 | 不放在 vue3 |
|------|-------------|
| `SheetCanvas` 模板 + **`useSheetCanvasView`**（绑 `SheetViewport`） | 公式栏、底部页签条、工具栏 DOM |
| **`useSheetInlineEdit`** + `FormulaRichInput` | 视口 controller 实现（应在 **view**） |
| `useSheet` → `sheet` + `revision` | `SheetChromeOptions`、antd / element |
| `provideSheetViewport` / upload inject | 十几个已删 composable 的复制粘贴 |
| `#context-menu` slot（坐标 + close，无 UI） | 页签 Pop、工具栏按钮 |
| **扩展挂载胶水**（无 antd/业务 UI）：`VueSheetOverlayRenderer`、`VueSheetBubbleRenderer`、`SheetExtensionViews`、`SheetBubbleMenusHost` + `useSheetBubbleFloating` | 具体扩展 UI：`Layer.vue`、`bubbleMenus/imageMenu`、图标按钮菜单 |
| `provideSheetViewport` / upload inject（数据通道） | `viewerjs`、预览类、`useCustomUpload`、文件下载 UI |

### headless 边界（改 vue3 前必答）

1. 新增 `.vue` 是否含 **业务菜单/图标/预览**？→ 只能放 **vue3-antd**（或未来 element 包）。  
2. 新增依赖是否仅为 **某一 UI 皮** 服务（如 viewerjs、antd）？→ 不进 vue3。  
3. vue3 允许的是 **与 UI 库无关的挂载/定位引擎**（对标 `@tiptap/vue-3` 的 NodeViewRenderer / BubbleMenu 插件，不是 `bubbleMenus/imageMenu` 本身）。  
4. **拿不准时**：先写 3 行说明落哪个包，对照下表，**不要先提交 vue3 代码**。

| 层级 | 示例 |
|------|------|
| core / extension-* | 命令、`renderer/`、`interaction/` Session，无 Vue |
| view | `SheetViewport`、`*Controller`，无 Vue |
| vue3 | `SheetCanvas`、`useSheetCanvasView`、`useSheetBubbleFloating`、扩展 **注册器** |
| vue3-antd | `Layer.vue`、`BubbleContainer`、`imageMenu`、`SheetPreviewImage` |

### 响应式要点

- `Sheet` 是普通 class，**不是** Vue 响应式对象。
- canvas 读 `sheet.state` 时，`computed` 必须依赖 **`revision`**（`useSheet` 在 `onUpdate` 时 `revision++`）。
- `SheetRenderer` 已废弃，等价于 `SheetCanvas`。

### useSheet 对外

```ts
const { sheet, revision, switchSheet, addSheet, isReady } = useSheet(options)
// 选区：sheet.value?.state.getSelection()
// 命令：sheet.value?.chain()...
```

## @speed-sheet/vue3-antd（UI 层）

### 组件拼装（SpeedSheet）

```
SheetFormulaBar
SheetToolbarHost → menus/toolbar
SheetCanvas（来自 vue3）
SheetTabBar → vuedraggable + a-dropdown（▾ click / 页签 contextmenu）→ SheetTabContextMenu overlay
```

### antd 使用

- 已配置 **unplugin-vue-components + AntDesignVueResolver**：模板里直接用 **`a-input`、`a-button`、`a-menu`** 等，无需手写 `import { Input } from 'ant-design-vue'`（图标仍从 `@ant-design/icons-vue` 引入）。
- 根节点加 class **`speed-sheet`**，全局业务变量见 `packages/vue3-antd/src/style/base.less`。

### 样式优先级（新功能按此执行）

布局与外观选型顺序（从高到低）：

1. **antd 布局组件**：`a-flex`、`a-space`、`a-row` / `a-col` 等承担排列、间距、对齐。
2. **UnoCSS**：原子类处理简单间距、宽高、`flex-1` 等（项目已引入 `uno.css`）。
3. **组件内 `<style scoped>`**：仅放 antd / Uno 不好表达的样式（页签形态、拖拽 ghost、`:deep` 覆盖等）。
4. **行内 `style`**：仅动态值（如 `tabAccentStyle` 的 `borderBottomColor`），禁止静态 hex 写死在模板。

**禁止**用 `<style scoped src="*.less">` 外链整块 chrome；各 SFC 样式写在各自文件内。共享变量放 `style/base.less`，不要共享组件局部 class 文件。

### CSS 变量（强制，含 bubbleMenus / 扩展 overlay）

antd 通过 `useAntdCssVars` 注入 **`--ant-*`**。所有 **vue3-antd 渲染 UI**（工具栏、菜单、`bubbleMenus/*`、扩展气泡内容）必须用变量，**禁止**在 `<style>` / Uno 里写死 `#hex`、`rgba(0,0,0,0.88)` 等。

#### 优先级

1. **`var(--ant-*)`** — 颜色、字号、间距、圆角、阴影、动效（见下表常用 token）。
2. **`var(--speed-*)`** — 仅当 antd 无合适语义时，在 **`packages/vue3-antd/src/style/base.less`** 的 `.speed-sheet` 下新增，命名 `--speed-{类别}-{用途}-{描述}-{序号}`，并 **fallback 到 ant**：  
   `color: var(--speed-color-border-gray, var(--ant-color-border));`
3. **组件 scoped 局部变量** — 仅该 SFC 独有、且不会复用时，可在根选择器上定义 `--sheet-dropdown-pick-item-height: 32px` 等，仍优先引用 `--ant-*`。

#### 常用 `--ant-*` 对照（写样式时直接抄）

| 用途 | 变量 |
|------|------|
| 正文 / 标题 | `--ant-color-text`、`--ant-font-size`、`--ant-font-weight-strong` |
| 弱化 / 图标灰 | `--ant-color-text-secondary`、`--ant-color-text-tertiary`、`--ant-color-text-quaternary` |
| 主色 / 选中底 | `--ant-color-primary`、`--ant-color-primary-bg` |
| 危险 / 删除 | `--ant-color-error`、`--ant-color-error-hover` |
| 容器底 / 浮层 | `--ant-color-bg-container`、`--ant-color-bg-elevated` |
| 悬停行 | `--ant-control-item-bg-hover` |
| 边框 | `--ant-color-border`、`--ant-color-border-secondary` |
| 间距 | `--ant-size-xs`、`--ant-size-sm`、`--ant-size-md` |
| 圆角 / 阴影 | `--ant-border-radius`、`--ant-box-shadow-secondary` |
| 过渡 | `--ant-motion-duration-mid` |

写法建议带 fallback：`padding: var(--ant-size-sm, 12px);`（token 未注入时仍可读）。

#### 扩展气泡 / `BubbleContainer`

- **外壳**（白底、padding、圆角、阴影）只由 `bubbleMenus/BubbleContainer.vue` 的 `.sheet-bubble-menu-wrapper` 用 `--ant-color-bg-elevated`、`--ant-border-radius`、`--ant-box-shadow-secondary` 承担。
- **子菜单**（`dropdownPickMenu`、`dropdownConfigMenu`、`imageMenu` 等）**不要**再设 `bubble-class-name` 去覆盖透明/无阴影/重复背景；只写**内容区** class（列表行高、选中态等）。
- 列表项示例：`color: var(--ant-color-text)`；`:hover { background: var(--ant-control-item-bg-hover); }`；`.is-selected { background: var(--ant-color-primary-bg); }`；勾选 `color: var(--ant-color-primary)`。
- 模板里 destructive 图标用 scoped class + `var(--ant-color-error)`，**不要** `text-[#bfbfbf]` / `text-[#f00]`；禁用态用 `--ant-color-text-quaternary`。

#### UnoCSS 与变量

- 简单布局可用 Uno（`flex`、`gap-2`）。
- **颜色、字号**优先 scoped + `var(--ant-*)`；若必须用 Uno 原子色，写成 `text-[var(--ant-color-text-tertiary)]`，禁止裸 hex。

#### 检查清单（样式）

- [ ] 无 `#` / `rgb(` 硬编码（动态单元格色除外）。
- [ ] 气泡子组件未重复 BubbleContainer 的背景/阴影。
- [ ] 新语义色已进 `base.less` 而非多个 SFC 各写一份。

### 菜单与气泡目录

```
menus/
  toolbar/          # 顶部工具栏
  insert/           # 插入菜单（仅入口；配置面板在 bubbleMenus）
  contextMenu/      # 单元格右键（antd Menu）
  sheetTabMenu/     # 页签右键 / ▾（antd Menu + SubMenu）
bubbleMenus/
  BubbleContainer.vue       # 统一浮层外壳（--ant-* 背景/阴影）
  dropdownConfigMenu/       # 下拉配置 + DropdownListPanel.vue
  dropdownPickMenu/         # 下拉取值列表
  imageMenu/                # 图片气泡
  dropdownMenus/index.vue   # SheetDropdown 扩展聚合挂载
components/
  SheetFormulaBar.vue
  SheetTabBar.vue
  SheetToolbarHost.vue
```

### core 工作表 API（页签相关）

- `addSheet` / `deleteSheet` / `renameSheet` / `duplicateSheet`
- `reorderSheets(orderedIds)` — 页签拖拽排序
- `getVisibleSheetIds` / `getActiveSheetId` / `setSheetTabColor` / `setSheetHidden`
- 切换页签用 **`useSheet.switchSheet`**（同步 `activeSheetId`），不要只调 `sheet.switchSheet` 而漏掉外壳状态。

### SpeedSheet defineExpose

只暴露：`sheet`、`switchSheet`、`addSheet`、`viewportEl`、`revision`。其余一律 `ref.sheet?.xxx`。

## 改代码时的检查清单 

0. **是否已读本文件 + `reference-architecture-before-code.mdc`？** 用户说「参考/对齐」时必须先架构方案，再编码。  
1. 新 UI 是否依赖 antd？→ 放 **vue3-antd**，不要放进 vue3。
2. 新数据能力？→ **core `Sheet`** + `notifyUpdate`。
3. canvas 是否要重绘？→ 确保 `revision` 递增或 watch `revision`。
4. 布局是否可用 `a-flex` / `a-space`？→ 优先 antd，其次 UnoCSS，再 scoped class。
5. 样式是否写在当前 SFC 的 `<style scoped>`？→ 禁止 `src` 外链组件样式；变量进 `base.less`。
6. 是否硬编码 hex / rgba？→ 改用 `var(--ant-*)` 或 `base.less` 的 `var(--speed-*)`；气泡勿重复 BubbleContainer 外壳样式。
